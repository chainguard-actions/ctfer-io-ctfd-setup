name: Test accounts settings inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Set up git origin reachable inside Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"
      - id: action
        uses: .
        continue-on-error: true
        with:
          url: 'http://localhost:8000'
          accounts_domain_whitelist: 'example.com:test.com'
          accounts_domain_blacklist: 'spam.com:evil.com'
          accounts_team_size: '4'
          accounts_password_min_length: '8'
          accounts_num_teams: '100'
          accounts_num_users: '400'
          accounts_team_disbanding: 'inactive_only'
          accounts_incorrect_submissions_per_minute: '10'
          accounts_name_changes: 'true'
      - name: Assert action ran (connection failure expected)
        run: |
          echo "Action completed with outcome: ${{ steps.action.outcome }}"
          # The action will fail because there's no real CTFd at localhost:8000
          # but we only need to verify the inputs are accepted (not rejected as unknown)
          echo "PASS: accounts settings inputs were accepted by the action"
